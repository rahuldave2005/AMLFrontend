import { Routes } from '@angular/router';
import { RuleConfigManagementComponent } from './rule-config-management.component';

export const RULE_CONFIG_MANAGEMENT_ROUTES: Routes = [
  {
    path: '',
    component: RuleConfigManagementComponent
  }
];